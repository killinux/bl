---
description: Take a screenshot of the Blender viewport
---

Capture a screenshot of the current Blender viewport:

```bash
cd $PROJECT_ROOT/cli
BLENDER_RELAY_API_KEY=mysecretkey python cli.py screenshot
```

After the screenshot is saved, read and display the image file to show the user what Blender looks like right now.
